
@[extern, private]
fn srand(n: i32)

@[extern, private]
fn rand(): i32

@[extern, private]
fn time(n: i32): i32

fn random(): i32
{
    srand(time(0))
    return rand()
}

fn rand_range(min: i32, max: i32) => random() % (max + 1 - min) + min
