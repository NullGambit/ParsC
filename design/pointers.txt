
fn main()
{
    // when using type inference a pointer must be annotated with *
    // otherwise will be a compile error 
    // this is just done for readability 
    var *ptr = #alloc<i32>()

    *ptr = 100
}