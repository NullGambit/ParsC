#include "stmt.hpp"

// static std::vector<pars::Statement> g_statements;
//
// size_t pars::new_stmt(Statement &&statement)
// {
// 	auto id = g_statements.size();
//
// 	g_statements.emplace_back(std::forward<Statement>(statement));
//
// 	return id;
// }
//
// pars::Statement & pars::get_stmt(size_t id)
// {
// 	return g_statements[id];
// }
