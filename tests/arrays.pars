import core.stdc.stdio

fn main()
{
    var arr1 = [1, 2, 3]
    var arr2 = [4, 5, 6]

    var result = arr1 + arr2

    for i in 0..arr1.length
    {
        printf("%d", result[i])

        if i < arr1.length - cast(u32)1
        {
            printf(", ")
        }
    }

    puts("")
}