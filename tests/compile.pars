
extern fn printf(s: str, c: char): i32
extern fn puts(value: str): i32

fn main()
{
    var fmt = "hello world %c"

    var x = add(sizeof(i32), 20)
    printf(fmt, '\n')
}

fn add(a: i32, b: i32) => a + b