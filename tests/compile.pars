import core.stdc.stdio

struct User
{
    name: str
    score: u32
}

struct Tag

fn main()
{
    var user = User{"john", 100}
    //var user2 = User{name: "henry", score: 50}
    //var user3: User = {name: "jack", score: 10}

    printf("name = %s, score = %d %d\n", user.name, user.score)
}