import std.io

fn main()
{
    fn add(a: i32, b: i32) => a + b

    var a = 1
    var b = 1

    var x = add(a, b)
    var y = mul(10, 20)

    println("hello")
}

fn mul(a: i32, b: i32) => yes(a) * b

fn yes(n: i32) => n

