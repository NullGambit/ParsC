import core.stdc.stdio

fn main()
{
    var user: {name: str, score: i32}
    var user2: {name: str, score: i32}

    user.score = 100
    user.name = "john"

    user2 = user

    printf("%s %d\n", user2.name, user2.score)
}