import core.stdc.stdio

alias Vec2 = [2]i32{x, y}

impl Vec2
{
    fn dot(other: Self) => self.x * other.x + self.y * other.y
}

fn main()
{
    let pos = Vec2[x: 10, y: 20]
    let pos2 = Vec2[x: 30, y: 40]

    let result = pos.dot(pos2)

    printf("d\n", result)
}